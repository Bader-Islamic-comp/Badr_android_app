# 8G heap plus 4G metaspace reserved more commit than a 16 GB machine can
# spare while the Unity export's native build forks parallel clang jobs, which
# failed with "LLVM ERROR: out of memory". Gradle does not need this much.
org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
